


**Summary**:
            Unchecked warnings are important. Don't ignore them.
            Every unchecked warning represent the potential for a ClassCasrException at run-time.
            Do your best to eliminate these warnings.
            If you can't eliminate unchecked warning and you can prove that the code that provoked it is typeface,
            suppress the warning with a @SuppressWarnings("unchecked") annotation in the narrowest possible scope.
            Record the rationale for your decision to suppress the warning in a comment.